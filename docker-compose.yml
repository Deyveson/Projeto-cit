version: '3.8'

services:
  mongo:
    image: mongo:7.0
    container_name: cit-mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: cit
    volumes:
      - mongo-data:/data/db
    networks:
      - cit-network

  backend:
    build: ./cit-backend
    container_name: cit-backend
    restart: always
    ports:
      - "8000:8000"
    environment:
      MONGODB_URL: mongodb://mongo:27017
      DATABASE_NAME: cit
      SECRET_KEY: your-super-secret-key-change-in-production-please
      ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 30
      MERCADOPAGO_ACCESS_TOKEN: TEST-422604264266269-060916-a9ecd2fa71807c4b292c427893b3104b-211982678
      MERCADOPAGO_PUBLIC_KEY: TEST-119771cd-08df-4688-983a-24ae0338d156
      MERCADOPAGO_USER_ID: 211982678
    depends_on:
      - mongo
    networks:
      - cit-network
    volumes:
      - ./cit-backend:/app

  frontend:
    build: ./cit-frontend
    container_name: cit-frontend
    restart: always
    ports:
      - "5173:5173"
    environment:
      VITE_API_BASE_URL: http://localhost:8000
    depends_on:
      - backend
    networks:
      - cit-network
    volumes:
      - ./cit-frontend:/app
      - /app/node_modules
    stdin_open: true
    tty: true

volumes:
  mongo-data:

networks:
  cit-network:
    driver: bridge
